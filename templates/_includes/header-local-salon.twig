<header class="main-header-top w-100 position-fixed top-0">
    <nav class="navbar navbar-expand-lg navbar-light py-3">
        <div class="container">
            <a class="navbar-brand" href="{{ url('/') }}">
                {% set logo = siteGlobals.image.eagerly().one() %}
                {% if logo and logo.extension == 'svg' %}
                    {{ svg(logo) }}
                {% else %}
                    <img class="img-fluid" src="{{ getenv('PRIMARY_SITE_URL') ~ '/static/images/halohair-logo.svg' }}" alt="{{ siteName }}">
                {% endif %}
            </a>
             <button class="navbar-toggler nav-icon" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span></span>
        <span></span>
        <span></span>
    </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto me-auto mb-2 mb-lg-0 gap-lg-3 gap-xl-4 gap-xxl-5">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ currentSite.baseUrl }}">Prices</a>
                    </li>
                    <li class="nav-item dropdown dmenu"> <a class="nav-link dropdown-toggle" href="{{ currentSite.baseUrl }}" id="dropdown01" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Service Menu <span class="dropdown-icon"></span></a>
                        {% include '_includes/benefits-menu' %}
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ currentSite.baseUrl }}">Meet our Stylists</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ currentSite.baseUrl }}">News</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ currentSite.baseUrl }}">Location</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ currentSite.baseUrl }}">Colour</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ currentSite.baseUrl }}">About Us</a>
                    </li>
                </ul>
                <div class="d-flex gap-3 align-items-center desktop-navbar-toggler">
                    {% if siteGlobals.bookButton %}<a class="apply-btn" href="{{ siteGlobals.bookButton.label }}">{{ siteGlobals.bookButton.label }}</a>{% endif %}
                    <div>
                        <button class="navbar-toggler nav-icon d-lg-block d-none top-0 me-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav2" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                            <span></span>
                            <span></span>
                            <span></span>
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </nav>
</header>